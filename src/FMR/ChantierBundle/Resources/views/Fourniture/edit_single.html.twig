<hr />
<h4>Modification de la fourniture {{ entity.id }}</h4>
<form action="{{ path('fourniture_update', { 'id': entity.id }) }}" method="post" class="form_inline_block" {{ form_enctype(edit_form) }}>
	<input type="hidden" name="_method" value="PUT" />
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-primary" type="submit">Enregistrer</button>
    </p>
</form>
<hr />
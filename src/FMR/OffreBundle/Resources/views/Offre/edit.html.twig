{# Projet TPI - Fabrice Maillefer #}
{% extends 'FMROffreBundle::layout.html.twig' %}
{% block title %}{{ parent() }} Modification de {{ entity.id }}{% endblock %}
{% block sidebar %}
    {{ parent() }}
    <li class="divider"></li>
	<li class="nav-header">Offre {{ entity.id }}</li>
	<li><a href="{{ path('offre_show', { 'id': entity.id }) }}">Détails de l'offre</a></li>
{% endblock %}
{% block content %}
<div class="page-header">
    <h1>Modification de l'offre {{ entity.id }}</h1>
    	<div style="text-align:right;">
		<a class="btn" href="{{ path('offre_show', { 'id': entity.id }) }}" >
            Retour aux d&eacute;tails de l'offre
        </a>
	</div>
</div>

<form action="{{ path('offre_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
	<input type="hidden" name="_method" value="PUT" />
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-primary" type="submit">Enregistrer</button>
    </p>
</form>

	<div class="form-actions">
		<a class="btn" href="{{ path('offre_show', { 'id': entity.id }) }}" >
            Retour aux d&eacute;tails de l'offre
        </a>
        <a class="btn btn-danger" href="{{ path('offre_delete', { 'id': entity.id }) }}">
            Supprimer
        </a>
</div>

{% endblock %}


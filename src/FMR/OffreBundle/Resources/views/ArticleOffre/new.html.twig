{% extends '::layout.html.twig' %}
{% block content -%}
	<div class="page-header">
	    <h1>Ajouter un article d'offre</h1>
    	<div style="text-align:right;">
			<a class="btn" href="{{ path('offre_show', { 'id': offre.id }) }}" >
	            Retour aux d&eacute;tails de l'offre
	        </a>
		</div>
	</div>

	{% include 'FMROffreBundle:ArticleOffre:new_single.html.twig' %}

    <div class="form-actions">
       	<a class="btn" href="{{ path('offre_show', { 'id': offre.id }) }}" >
	            Retour aux d&eacute;tails de l'offre
	    </a>
	</div>

{% endblock %}

{# Projet TPI - Fabrice Maillefer #}
{% extends 'FMRFactureBundle::layout.html.twig' %}
{% block title %}{{ parent() }} Modification de {{ entity.id }}{% endblock %}
{% block sidebar %}
    {{ parent() }}
    <li class="divider"></li>
	<li class="nav-header">Facture {{ entity.id }}</li>
	<li><a href="{{ path('facture_show', { 'id': entity.id }) }}">Détails de la facture</a></li>
{% endblock %}
{% block content %}
<div class="page-header">
    <h1>Modifier une facture</h1>
    	<div style="text-align:right;">
			<a class="btn" href="{{ path('facture_show', { 'id': entity.id }) }}" >
	            Retour aux d&eacute;tails de la facture
	        </a>
		</div>
</div>

<form action="{{ path('facture_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
	<input type="hidden" name="_method" value="PUT" />
    {{ form_widget(edit_form) }}
    <p>
        <button class="btn btn-primary" type="submit">Enregistrer</button>
    </p>
</form>

	<div class="form-actions">
		<a class="btn btn-primary" href="{{ path('facture_show', { 'id': entity.id }) }}" >
            Retour aux d&eacute;tails de la facture
        </a>
        <a class="btn btn-danger" href="{{ path('facture_delete', { 'id': entity.id }) }}">
            Supprimer
        </a>
</div>

{% endblock %}

